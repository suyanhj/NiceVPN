# 任务清单 — vpn-ui-firewall-page-2026-04-20

> 本变更为**文档/索引**向；不跟踪具体代码行修改。

- [x] 新增本变更目录：`proposal.md` / `design.md` / `tasks.md` / `.openspec.yaml`
- [x] 在 `design.md` 中写清：双 Tab、中心 JSON 与对端工作副本、关键模块引用、与既有 openspec 变更的互链
- [x] **可选**：`vpn-bugfix-2026-04-03-r3` 已补 **§2.2**（`filter` 快照/回滚）与 `tasks` **T2b**（与 `IptablesManager.rebuild_rules` 对齐）
- [x] 后续对规则页做大改动时，**同步**更新 **本条** `design.md` §1–3（**2026-04-28**：已对齐「规则页仅 `VPN_FORWARD` / 启动全量 `rebuild_iptables`」）
- [x] **2026-04-29**：`design.md` §1 已与实现对齐：中心策略 **outlined/dense + 回车** 搜索、**`DRAG TO CHANGE PRIORITY` / 条数** 在**带边框卡片外**作页脚汇总（双 Tab 同构）
